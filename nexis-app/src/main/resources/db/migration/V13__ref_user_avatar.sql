-- =====================================================================
-- V13 : avatar de profil utilisateur (clé d'avatar choisie dans une liste).
-- =====================================================================
ALTER TABLE ref_user ADD COLUMN avatar VARCHAR(20);
