# Profil "dev" — activé par défaut au lancement de l'application (voir Application.java).
# Connexion à la base PostgreSQL locale.
datasources.default.url=jdbc:postgresql://localhost:5432/nexis
datasources.default.username=postgres
datasources.default.password=postgres
